apiVersion: v1
kind: Service
metadata:
  name: shop-core-service-nodeport
spec:
  type: NodePort
  selector:
    app: shop-core-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
